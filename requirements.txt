# Frontend (NiceGUI – reines Python, keine Node-Toolchain).
# Mindestens 3.6: ab dort hat ui.html den Parameter `sanitize`, den der
# Belegscanner zwingend auf False setzen muss (sonst fliegen <video>/<canvas>
# aus dem Markup). Unter 4 gepinnt, weil dort Breaking Changes zu erwarten sind.
nicegui>=3.6,<4

# PDF-Erzeugung (amtliches Formular).
pymupdf>=1.24

# QR-Code für 2FA (Google Authenticator) – reines Python.
segno>=1.6

# Empfohlen (auf dem Server installiert): perspektivische Entzerrung der Belege
# aus den im Scanner gesetzten Ecken (receipts.crop_quad) und die automatische
# Erkennung beim Datei-Upload (receipts.autocrop). Fehlen sie, wird auf einen
# achsenparallelen Zuschnitt per PyMuPDF zurueckgefallen – schraeg fotografiert
# bleibt das Bild dann schief. Nicht hart eingetragen, weil das Rad rund 60 MB
# gross ist und die App ohne es lauffaehig bleibt.
# opencv-python-headless
# numpy

# Web Push an die Handys (AP7). Verschluesselung nach RFC 8291 (ECDH + HKDF +
# AES-GCM) und VAPID – bewusst keine Eigenbau-Stelle: ein Fehler darin zeigt
# sich nicht als Absturz, sondern als "kommt bei manchen Geraeten still nicht
# an". Zieht cryptography nach.
pywebpush>=2.0
openpyxl==3.1.5  # Auszahlungsberichte von Booking (B11)
