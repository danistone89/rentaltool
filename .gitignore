# Secrets & personenbezogene Daten – NIEMALS einchecken
config.json

# PDF-Vorlage enthält Betreiberdaten + Kassenzeichen
templates/anmeldung_blank.pdf
templates/*.pdf

# Erzeugte / temporäre PDFs
*.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Unterschrift (personenbezogen)
assets/

# Archiv (echte Steuerdokumente, personenbezogen)
archive/

# NiceGUI Session-Storage (enthält Login-Sessions)
.nicegui/

# Arbeitszeiten (personenbezogen: Zeiten + Standorte)
worklog.json

# Reinigung & Qualität (betrieblich/personenbezogen)
checklists.json
inventory.json
cleanings.json
damages.json
restock.json
media/

# Buchungs-Zuweisungen (betrieblich)
assignments.json

# Belege/Rechnungen (betrieblich/personenbezogen)
receipts.json

# OpenCV.js (~10 MB) wird per tools/fetch_opencv.sh geholt, nicht eingecheckt
static/opencv.js
